#!/usr/bin/env bash

set -euo pipefail

repository_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

if ! command -v cargo >/dev/null 2>&1; then
  echo "Error: Cargo is required to run the sender." >&2
  exit 1
fi

cd "$repository_root/sender"
cargo run --release -- "$@"
