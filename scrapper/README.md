# scrapper

To install dependencies:

```bash
bun install
```

To build:

```bash
bun run build
```

The generated userscript is written to `dist/scrapper.user.js`.

This project was created using `bun init` in bun v1.3.13. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
